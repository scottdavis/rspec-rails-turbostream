RSpec.describe Rspec::Rails::Turbostream do
  it "has a version number" do
    expect(Rspec::Rails::Turbostream::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
